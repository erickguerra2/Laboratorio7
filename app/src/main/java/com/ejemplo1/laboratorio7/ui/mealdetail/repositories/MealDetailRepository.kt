package com.ejemplo1.laboratorio7.ui.mealdetail.repositories
/*
import com.ejemplo1.laboratorio7.networking.webservices.MealsWebService
import com.ejemplo1.laboratorio7.networking.response.mealdetail.MealDetail

class MealDetailRepository(private val webService: MealsWebService = MealsWebService()) {

    suspend fun getMealDetail(category: String): List<MealDetail> {
        return webService.getMealDetail(mealId).mealdetail
    }
}

class MealDetailRepository(private val webService: MealsWebService = MealsWebService()) {

    suspend fun getMealDetail(mealId: String): MealDetail? {
        val response: MealDetailResponse = webService.getMealDetail(mealId)
        return response.meals.firstOrNull()
    }
}
*/